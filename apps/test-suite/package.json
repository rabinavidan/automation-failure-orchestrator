{
  "name": "@orchestrator/test-suite",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "clean": "rm -rf test-results playwright-report allure-results dist"
  },
  "dependencies": {
    "@orchestrator/shared-types": "*"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "allure-playwright": "^3.0.0",
    "typescript": "^7.0.2",
    "tsx": "^4.19.0"
  }
}
